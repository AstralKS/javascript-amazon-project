export function formatCurrency(priceCents) {
    return (priceCents / 100).toFixed(2);
  }

  //export default formatCurrency; // this mean i only export this function when this library is called upon